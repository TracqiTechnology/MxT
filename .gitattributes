# Golden-test inputs and generated expectations are byte-reviewed artifacts.
# Keep their line endings stable so exact hashes are identical on every runner.
src/test/resources/logs/*.csv text eol=lf
src/test/resources/golden/**/*.json text eol=lf
tools/golden/*.py text eol=lf
